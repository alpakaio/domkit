{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "NPM Install",
			"command": "npm",
			"args": ["i"],
			"options": {
				"cwd": "${workspaceRoot}"
			}
		},
		{
			"label": "Build All",
			"command": "npm",
			"args": ["run build"],
			"options": {
				"cwd": "${workspaceRoot}"
			}
		}
	]
}